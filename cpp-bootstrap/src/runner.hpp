#pragma once

#include <vector>
#include <string>
#include "program.hpp"

class Runner {
public:
	Runner(void) {
	}

	void run(const Program &program, const std::vector<std::string> &arguments) {
	}
};